export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum Status {
  PENDING  = 'PENDING',
  PROGRESS = 'PROGRESS',
  COMPLETED = 'COMPLETED'
}

export enum Priority {
  LOW  = 'LOW',
  MEDIUM = 'MEDIUM',
  HIGH = 'HIGH'
}

export enum Status_project {
  ESTABLISHED = 'ESTABLISHED',
  PROGRESS = 'PROGRESS',
  COMPLETED = 'COMPLETED'
}