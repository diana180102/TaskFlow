export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum Status {
  PENDING,
  PROGRESS,
  COMPLETED
}

export enum Priority {
  LOW,
  MEDIUM,
  HIGH
}

export enum Status_project {
  ESTABLISHED = 'ESTABLISHED',
  PROGRESS = 'PROGRESS',
  COMPLETED = 'COMPLETED'
}