export enum Role {
  ADMIN,
  USER
}

export enum Status {
  PENDING,
  PROGRESS,
  COMPLETED
}

export enum Priority {
  LOW,
  MEDIUM,
  HIGH
}

export enum Status_project {
  ESTABLISHED,
  PROGRESS,
  COMPLETED
}